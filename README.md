# Inversify
Manipulation with multiInject, inject, tagged, named. Single decoration with wide usage.

Require:

- `NodeJS, npm`
- `Linux system`

Install

- `npm install`

Run example

- `npm run dev`
